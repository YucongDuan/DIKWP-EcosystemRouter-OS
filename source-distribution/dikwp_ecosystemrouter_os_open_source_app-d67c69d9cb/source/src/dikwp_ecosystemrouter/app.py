# Optional Streamlit app. Imported only when streamlit is installed.
def main():
    import streamlit as st
    st.title("DIKWP EcosystemRouter OS")
    st.write("Upload ecosystem and partner manifests, then run the CLI evaluator for an offline strategic report.")

if __name__ == "__main__":
    main()
