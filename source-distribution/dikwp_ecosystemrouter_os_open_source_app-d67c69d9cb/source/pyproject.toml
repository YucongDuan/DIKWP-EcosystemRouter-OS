[project]
name = "dikwp-ecosystemrouter"
version = "0.1.0"
description = "Strategic open-source ecosystem routing and benefit-capture control plane for DIKWP projects."
authors = [{ name = "Yucong Duan / DIKWP Ecosystem" }]
readme = "README.md"
requires-python = ">=3.9"
license = { text = "Apache-2.0" }
dependencies = []

[project.optional-dependencies]
app = ["streamlit"]

[project.scripts]
dikwp-ecosystemrouter = "dikwp_ecosystemrouter.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
